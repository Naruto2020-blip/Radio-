package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Retro-modern glowing palette
val DarkCharcoal = Color(0xFF121212)
val MatteSlate = Color(0xFF1E1E1E)
val BorderGrey = Color(0xFF2E2E2E)

val AmberGlow = Color(0xFFFFB300)     // Warm golden dial
val AmberAccent = Color(0xFFFFCC80)   // Dial highlights
val TealGlow = Color(0xFF00E5FF)      // Frequency indicator / Cyan accent
val DarkRed = Color(0xFFD32F2F)       // Recording / Live dot red

// M3 standard tokens
val PrimaryDark = AmberGlow
val SecondaryDark = Color(0xFF37474F)
val TertiaryDark = TealGlow
val BackgroundDark = DarkCharcoal
val SurfaceDark = MatteSlate
val OnPrimaryDark = Color.Black
val OnSecondaryDark = Color.White
val OnBackgroundDark = Color(0xFFE0E0E0)
val OnSurfaceDark = Color(0xFFE0E0E0)
